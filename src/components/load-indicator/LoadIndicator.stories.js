import React from 'react'
import { LoadIndicator } from '../'

export default {
  title: 'LoadIndicator component',
  component: LoadIndicator
}

const template = () => <LoadIndicator />

export const FirstStory = template.bind({})
