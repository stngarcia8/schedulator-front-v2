import React from 'react'
import NoTask from './NoTask'

export default {
  title: 'No task screen',
  component: NoTask
}

const template = () => <NoTask />

export const FirstStory = template.bind({})
